CREATE OR REPLACE TYPE t_prodfcst_var_rec AS OBJECT (
    FORECAST_ID VARCHAR2(32),
    OBJECT_ID VARCHAR2(32),
    FROM_MONTH DATE,
    TO_MONTH DATE,
    VARIABLE_CODE VARCHAR2(32),
    VARIABLE_NAME VARCHAR2(32),
    SORT_ORDER NUMBER,
    OIL NUMBER,
    GAS NUMBER,
    WAT NUMBER,
    COND NUMBER,
    GL NUMBER,
    DL NUMBER,
    WI NUMBER,
    GI NUMBER,
    SI NUMBER,
    CI NUMBER);