CREATE OR REPLACE FORCE EDITIONABLE VIEW "V_CASCADE" ("CALC_TYPE", "EXECUTION_ORDER", "OBJECT_ID", "CODE", "VERIFICATIONSTATUS_CODE", "VERIFICATIONTEXT_CODE", "EQUATION", "CALC_METHOD", "MEASURE", "NETMASS", "NETMASSUNIT", "NETVOLUME", "NETVOLUMEUNIT", "NETENERGY", "NETENERGYUNIT", "SPLITSHARE", "DAYTIME", "STATUS", "SPLIT_TYPE", "RECORD_STATUS", "CREATED_BY", "CREATED_DATE", "LAST_UPDATED_BY", "LAST_UPDATED_DATE", "REV_NO", "REV_TEXT", "APPROVAL_BY", "APPROVAL_DATE", "APPROVAL_STATE", "REC_ID") AS 
  (
SELECT
	   NULL CALC_TYPE,
	   NULL EXECUTION_ORDER,
	   NULL OBJECT_ID,
	   NULL CODE,
     NULL VERIFICATIONSTATUS_CODE,
     NULL VERIFICATIONTEXT_CODE,
     NULL EQUATION,
	   NULL CALC_METHOD,
	   NULL MEASURE,
 	   NULL NETMASS,
	   NULL NETMASSUNIT,
	   NULL NETVOLUME,
	   NULL NETVOLUMEUNIT,
	   NULL NETENERGY,
	   NULL NETENERGYUNIT,
	   NULL SPLITSHARE,
	   NULL DAYTIME,
	   NULL STATUS,
     NULL SPLIT_TYPE,
     NULL RECORD_STATUS,
     NULL CREATED_BY,
     NULL CREATED_DATE,
     NULL LAST_UPDATED_BY,
     NULL LAST_UPDATED_DATE,
     NULL REV_NO,
     NULL REV_TEXT,
     NULL APPROVAL_BY,
     NULL APPROVAL_DATE,
     NULL APPROVAL_STATE,
     NULL REC_ID
	FROM STREAM_ITEM
	WHERE 1 = 0
)