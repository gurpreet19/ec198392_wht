CREATE OR REPLACE FORCE EDITIONABLE VIEW "V_DSF_DETAIL_NONE" ("REFERENCE_ID", "TYPE", "DIRECTION", "CA_CLASS", "OBJECT_ID", "DATASET", "ID", "MAPPING_TYPE", "MAPPING_ID", "DAYTIME", "OTHER_TYPE", "OTHER_REF_ID", "OTHER_MAPPING_TYPE", "OTHER_ID", "RECORD_STATUS", "CREATED_BY", "CREATED_DATE", "LAST_UPDATED_BY", "LAST_UPDATED_DATE", "REV_NO", "REV_TEXT", "TO_ID", "FROM_ID", "CONNECTION_ID", "COMMENTS") AS 
  SELECT
       to_char(null) reference_id,
       to_char(null) type,
       'OUT' direction,
       'CONT_JOURNAL_SUMMARY' CA_CLASS,
       --ATTRIBUTE CA_ATTRIBUTE,
       to_char(null)  OBJECT_ID,
       to_char(null) dataset,
       to_char(null) id,
       to_char(null) mapping_type,
       to_char(null) mapping_id,
       (null) DAYTIME,
       to_char(null) other_type,
       to_char(null) other_ref_id,
       to_char(null) other_mapping_type,
       to_char(null) other_id,
        to_char(null) record_status,
        to_char(null) created_by,
        to_date(null) created_date,
        to_char(null) last_updated_by,
        to_date(null) last_updated_date,
        to_number(null) rev_no,
        to_char(null) rev_text,
        to_char(null) TO_ID,
        to_char(null)From_Id,
        to_char(null) Connection_Id,
        to_char(null) comments
  FROM prosty_codes
  where CODE_TYPE = 'DUAL'