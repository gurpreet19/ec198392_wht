CREATE OR REPLACE EDITIONABLE TRIGGER "D_FORECAST_EVENT" 
BEFORE DELETE ON FCST_WELL_EVENT
FOR EACH ROW
DECLARE
BEGIN
     DELETE FROM FCST_WELL_EVENT_ALLOC
     WHERE event_no = :old.event_no;
     DELETE FROM TEMP_FCST_WELL_EVENT_ALLOC
     WHERE event_no = :old.event_no;
END;
